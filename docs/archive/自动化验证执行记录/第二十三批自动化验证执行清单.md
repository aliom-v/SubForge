# SubForge 第二十三批自动化验证执行清单

## 1. 文档目的

本文档用于固定第二十三批的文档增强结果，重点补齐“常见错误怎么对照”“预览和公开订阅不一致时怎么分支排查”“部署后去哪里找对应说明”这三类入口。

本轮完成的三类工作：

1. 为 `docs/节点协议示例库.md` 补 `ss` / `hysteria2` 常见报错对照
2. 为 `docs/导入成功但订阅没变化排障指南.md` 补“预览有变化但公开订阅没变化”的分支排查
3. 为 `docs/部署指南.md` 增加到示例库与排障指南的直接入口，并同步 smoke 断言

当前进展：`done`

---

## 2. 本轮范围

### 2.1 本轮已补

- `docs/archive/自动化验证执行记录/第二十三批自动化验证执行清单.md`
- `docs/节点协议示例库.md`
- `docs/导入成功但订阅没变化排障指南.md`
- `docs/部署指南.md`
- `docs/已知问题与修复计划.md`
- `docs/自动化验证与CI计划.md`
- `.omx/plans/2026-03-07-subforge-roadmap.md`
- `scripts/smoke-check.mjs`

### 2.2 本轮不补

- 新协议实现
- 新协议测试
- `hysteria2` 多端口 `authority`
- 远程节点源同步
- Clash / sing-box 全量配置导入

---

## 3. 实际落地内容

### 3.1 协议示例库补错误语义对照

已落地到：

- `docs/节点协议示例库.md`

本轮新增：

- `ss` 常见字段缺失与复杂 plugin 字段 unsupported 对照
- `hysteria2` 缺失主字段、非法 `obfs`、未知 query 参数、错误类型字段对照
- 把“可复制样例”和“常见错误语义”放在同一文档里，便于运营和排障快速切换

### 3.2 排障指南补 preview -> public 分支

已落地到：

- `docs/导入成功但订阅没变化排障指南.md`

本轮新增：

- “预览有变化但公开订阅没变化”单独分支
- 优先检查当前 token、目标类型、公开订阅地址、客户端缓存、重置 token 后旧地址等实际问题
- 把“后台预览正常”和“公开订阅异常”拆成独立排查路径，避免继续混在一条总链路里

### 3.3 部署文档补入口链接

已落地到：

- `docs/部署指南.md`

本轮新增：

- 从部署文档直接跳转到协议示例库
- 从部署文档直接跳转到“导入成功但订阅没变化”排障指南
- 部署完成后从“环境问题”切换到“使用链路问题”时，有明确入口

### 3.4 smoke 校验同步

已落地到：

- `scripts/smoke-check.mjs`

本轮新增断言：

- 第二十三批文档存在且包含三类增强项
- 协议示例库包含“常见报错对照”
- 排障指南包含“预览有变化但公开订阅没变化”分支
- 部署指南包含到示例库与排障指南的入口

---

## 4. 完成标准

本轮完成后已满足：

- 运维或排障时，不需要先翻代码就能看到 `ss` / `hysteria2` 常见错误语义
- “预览正常但公开订阅异常”已有独立分支，不再混在总链路里
- 部署文档能把人直接带到后续使用与排障入口
- smoke 已对第二十三批与新增文档关键字做最小保护

建议下一步继续：

1. 继续补 `hysteria2` 多端口 `authority` 与更复杂变体
2. 继续补下一批主流协议导入
3. 继续补更细的错误矩阵与发布前回归
