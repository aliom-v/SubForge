# SubForge 第二十五批自动化验证执行清单

## 1. 文档目的

本文档用于固定第二十五批的文档深化结果，重点解决“已经有导航页，但接口约定、节点字段和常见错误仍需要到代码里反查”的问题。

本轮完成的四类工作：

1. 新增 `docs/API参考与接口约定.md`
2. 新增 `docs/节点字段字典.md`
3. 新增 `docs/常见错误与返回语义.md`
4. 为 `docs/文档导航与阅读顺序.md` 补开发 / 运维 / 运营三类阅读路径，并同步入口与 smoke 断言

当前进展：`done`

---

## 2. 本轮范围

### 2.1 本轮已补

- `docs/第二十五批自动化验证执行清单.md`
- `docs/API参考与接口约定.md`
- `docs/节点字段字典.md`
- `docs/常见错误与返回语义.md`
- `docs/文档导航与阅读顺序.md`
- `README.md`
- `docs/部署指南.md`
- `docs/节点管理与订阅使用说明.md`
- `docs/已知问题与修复计划.md`
- `docs/自动化验证与CI计划.md`
- `.omx/plans/2026-03-07-subforge-roadmap.md`
- `scripts/smoke-check.mjs`

### 2.2 本轮不补

- 新协议实现
- 新测试实现
- `hysteria2` 多端口 `authority`
- 远程节点源同步
- Clash / sing-box 全量配置导入

---

## 3. 实际落地内容

### 3.1 API 参考收口

已落地到：

- `docs/API参考与接口约定.md`

本轮新增：

- 管理端 API、公开订阅、预览接口的统一入口清单
- 成功 / 失败返回包络约定
- 管理员 Bearer 鉴权、预览缓存头、公开订阅文本响应等易混淆语义
- 关键写接口的请求字段范围与典型成功返回

### 3.2 节点字段字典收口

已落地到：

- `docs/节点字段字典.md`

本轮新增：

- 节点基础字段、`credentials`、`params` 的统一解释
- `vless` / `trojan` / `vmess` / `ss` / `hysteria2` 当前常见字段映射
- `hy2` 归一化、`sourceType` / `sourceId` 边界、metadata `object | null` 语义
- 结构化向导、分享链接导入、原始 JSON 三者的对照关系

### 3.3 常见错误与返回语义收口

已落地到：

- `docs/常见错误与返回语义.md`

本轮新增：

- `AppErrorShape`、状态码与错误码的统一口径
- 高概率输入校验错误、鉴权错误、模板错误、协议字段错误对照
- `/api/node-import/preview` 只预览不落库、`/api/users/:id/nodes` 为整组替换、`/s/:token/:target` 返回纯文本等成功语义说明

### 3.4 导航补角色阅读路径

已落地到：

- `docs/文档导航与阅读顺序.md`

本轮新增：

- 开发者阅读路径
- 部署 / 运维阅读路径
- 运营 / 后台使用阅读路径
- 新增到 API 参考、字段字典、错误语义三份文档的直接跳转

### 3.5 smoke 校验同步

已落地到：

- `scripts/smoke-check.mjs`

本轮新增断言：

- 第二十五批文档存在且包含三份专题文档与角色化导航范围
- API 参考、字段字典、错误语义三份文档存在且包含关键短语
- 统一导航页包含开发 / 运维 / 运营阅读路径
- README、部署指南、节点使用说明已补到三份专题文档的入口

---

## 4. 完成标准

本轮完成后已满足：

- 接手项目时不需要先翻代码才能知道 API 路由、返回包络和常见请求体
- 节点字段不再散落在 README、使用说明和实现文件之间
- 常见错误码、状态码和排查语义有统一入口
- 统一导航页已经能按角色给出最小阅读顺序
- `npm run test:smoke` 可以继续对文档入口和关键短语做最小保护

建议下一步继续：

1. 继续补 `hysteria2` 多端口与更复杂链接变体
2. 继续补下一批主流协议导入
3. 继续把错误矩阵和发布前回归从文档保护推进到更多实现级测试
