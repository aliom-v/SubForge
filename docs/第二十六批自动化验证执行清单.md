# SubForge 第二十六批自动化验证执行清单

## 1. 文档目的

本文档用于固定第二十六批的文档补完结果，重点解决“专题文档已经有了，但还缺状态码速查、完整示例、导入映射对照和上线前验收清单”的问题。

本轮完成的五类工作：

1. 为 `docs/API参考与接口约定.md` 增加按资源分组的状态码速查
2. 为 `docs/API参考与接口约定.md` 增加完整请求 / 响应示例
3. 为 `docs/节点字段字典.md` 增加 `ss` / `hysteria2` 导入字段映射表
4. 为 `docs/常见错误与返回语义.md` 增加错误跳转索引
5. 新增 `docs/发布前检查清单.md`，并同步导航、入口和 smoke 断言

当前进展：`done`

---

## 2. 本轮范围

### 2.1 本轮已补

- `docs/第二十六批自动化验证执行清单.md`
- `docs/API参考与接口约定.md`
- `docs/节点字段字典.md`
- `docs/常见错误与返回语义.md`
- `docs/发布前检查清单.md`
- `docs/文档导航与阅读顺序.md`
- `README.md`
- `docs/部署指南.md`
- `docs/节点管理与订阅使用说明.md`
- `docs/已知问题与修复计划.md`
- `docs/自动化验证与CI计划.md`
- `.omx/plans/2026-03-07-subforge-roadmap.md`
- `scripts/smoke-check.mjs`

### 2.2 本轮不补

- 新协议实现
- 新测试实现
- `hysteria2` 多端口 `authority`
- 远程节点源同步
- Clash / sing-box 全量配置导入

---

## 3. 实际落地内容

### 3.1 API 文档补状态码与完整示例

已落地到：

- `docs/API参考与接口约定.md`

本轮新增：

- setup / users / nodes / templates / rule-sources / preview / subscription 的状态码速查
- `POST /api/setup/bootstrap` 完整请求 / 响应示例
- `POST /api/node-import/preview` 完整请求 / 响应示例
- `POST /api/nodes` 完整请求 / 响应示例
- `GET /s/:token/:target` 纯文本响应示意

### 3.2 字段字典补导入映射表

已落地到：

- `docs/节点字段字典.md`

本轮新增：

- `ss` 分享链接导入字段 -> 最终 `credentials` / `params` 对照
- `hysteria2` / `hy2` 分享链接导入字段 -> 最终 `credentials` / `params` 对照
- 对 `port` 默认值、`alpn`、`pinSHA256`、`insecure`、复杂 plugin 的写实说明

### 3.3 错误文档补跳转索引

已落地到：

- `docs/常见错误与返回语义.md`

本轮新增：

- “看到这个错误先去看哪份文档”索引
- 从 setup、字段校验、导入排障、发布验收四类问题直接跳到对应文档

### 3.4 发布前检查清单

已落地到：

- `docs/发布前检查清单.md`

本轮新增：

- 基础环境检查
- 模板 / 用户 / 节点 / 绑定检查
- 导入与 metadata 检查
- 预览与公开订阅的 `miss -> hit` 验收
- 发布失败时的优先跳转索引

### 3.5 smoke 校验同步

已落地到：

- `scripts/smoke-check.mjs`

本轮新增断言：

- 第二十六批文档存在且覆盖状态码、示例、字段映射、错误索引与发布前检查清单
- API 参考包含状态码速查和完整请求 / 响应示例
- 节点字段字典包含导入字段 -> 最终 metadata 对照
- 常见错误文档包含错误跳转索引
- 发布前检查清单已被 README、导航、部署文档接入

---

## 4. 完成标准

本轮完成后已满足：

- 接口文档不再只停留在“路由列表”，而是补上了可直接照着请求的示例
- `ss` / `hysteria2` 的导入映射不需要继续翻实现文件确认
- 常见错误不再只有错误码说明，还能直接跳到下一份该看的文档
- 发布前已经有一份固定的最小验收单
- `npm run test:smoke` 可以继续保护这些文档入口和关键短语

建议下一步继续：

1. 继续补 `hysteria2` 多端口和更复杂链接变体
2. 继续补下一批主流协议导入
3. 继续把发布前检查清单里的关键项推进到更多自动化回归
