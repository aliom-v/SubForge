# SubForge 实施方案

## 1. 文档目的

本文档用于把当前单用户托管模式的实现方向收敛为一份**长期可维护的设计基线**，重点回答这些问题：

- SubForge 要解决什么问题、明确不做什么
- 仓库结构、模块边界和职责如何划分
- D1 / KV / Worker / Web 分别承担什么职责
- API、数据模型、缓存和同步链路应如何设计
- 后续阶段应按什么原则演进

如果你只是自己部署、自己使用，这份文档通常可以直接跳过；它属于仓库内部设计参考，不是单人使用说明。

本文档不负责维护：

- 当前已实现功能清单
- 本地 / Cloudflare 的具体部署步骤
- 发布记录与版本变更摘要

对应关系如下：

- 当前实现现状看 `README.md`
- 具体部署步骤看 `docs/部署指南.md`
- 发布记录看 `CHANGELOG.md`
- 当前执行建议看 `docs/仓库清理与收敛计划.md`

本文档默认面向单仓库开发，优先保证：

- 结构清晰
- 边界稳定
- 能从 MVP 平滑演进

---

## 2. 项目定义

### 2.1 项目定位

SubForge 是一个部署在 Cloudflare 上的单用户托管订阅系统，前端默认只暴露“导入节点 -> 调整节点 -> 生成托管 URL”这条主流程。系统内部仍会维护：

- 节点
- 用户
- 订阅 token
- 模板骨架
- 远程订阅自动同步源
- 用户节点绑定
- 内部审计与同步状态

并输出：

- Mihomo 订阅
- sing-box 订阅

### 2.2 核心原则

SubForge 必须坚持以下原则：

1. 不运行代理核心，只做配置管理与订阅分发
2. 不依赖常驻进程，优先使用 Cloudflare Worker + Cron
3. 不做动态脚本引擎，所有转换逻辑都显式实现
4. 先做模板化、配置化能力，再做高级扩展
5. 优先保证订阅稳定输出，再优化前端管理体验

### 2.3 第一阶段范围

下面列的是设计基线中的 MVP 范围；实际是否已经落地，请以 `README.md` 与 `CHANGELOG.md` 为准：

- 管理员登录
- 节点 CRUD
- 自动维护单用户托管身份与 token
- 自动维护托管模板骨架
- 保存和同步远程订阅自动同步源
- 生成 Mihomo 订阅
- 生成 sing-box 订阅
- 基于 KV 的预览 / 订阅缓存
- 自动同步源状态诊断
- 内部审计写入

### 2.4 非目标

以下能力不在 MVP 范围内：

- 代理测速
- 代理常驻运行
- 用户自定义 JS 执行
- 多租户计费系统
- 复杂权限系统
- 超大规模实时批处理

---

## 3. 技术选型

### 3.1 总体选型

- 前端：React + Vite + TypeScript
- API：Cloudflare Workers + TypeScript
- 数据库：Cloudflare D1
- 缓存：Cloudflare KV
- 定时任务：Cloudflare Cron Triggers
- 部署：Wrangler
- 代码组织：Monorepo
- 包管理：`npm workspaces`

### 3.2 选型理由

选择 `npm workspaces` 而不是额外引入复杂 monorepo 工具，原因是：

- 当前项目规模不大，先降低初始化成本
- Node / Vite / Wrangler 对 npm 支持最直接
- 后续如果包数量变多，再迁移到更强的 workspace 管理工具也不迟

### 3.3 可选增强

以下能力作为后续增强项保留：

- R2：D1 备份归档
- Cloudflare Access：保护管理入口
- GitHub Actions：自动部署与检查
- Sentry / Logpush：错误监控

---

## 4. 仓库结构设计

建议采用如下目录：

```text
.
├── apps/
│   ├── web/                # 单用户前端主界面
│   └── worker/             # Worker API
├── packages/
│   ├── core/               # 订阅生成、同步、缓存失效等核心逻辑
│   └── shared/             # 类型、常量、schema、通用工具
├── migrations/             # D1 migration SQL
├── scripts/                # 本地初始化与辅助脚本
├── docs/                   # 项目文档
├── package.json            # workspace 根配置
├── wrangler.toml           # Worker 部署配置
└── README.md
```

### 4.1 目录职责

- `apps/web`：单用户节点主界面、首次安装向导、管理员登录与托管结果展示
- `apps/worker`：HTTP API、鉴权、订阅输出、Cron 任务
- `packages/core`：业务服务层，不直接依赖 UI
- `packages/shared`：共享 DTO、zod schema、常量枚举、工具函数
- `migrations`：数据库结构演进
- `scripts`：初始化 demo 数据、导入样例、开发辅助脚本
- `docs`：实施文档、接口约定、里程碑计划

---

## 5. 系统架构

### 5.1 架构概览

系统由四个核心层组成：

1. Web 前端主界面
2. Worker API
3. D1 数据存储
4. KV 缓存层

外加一个定时调度层：

5. Cron 任务

### 5.2 请求流

#### 前端管理请求流

1. 管理员登录前端主界面
2. 前端调用 Worker API
3. Worker 完成参数校验和鉴权
4. Worker 写入或查询 D1
5. 变更成功后触发缓存失效
6. 前端刷新节点主界面状态或托管预览结果

#### 订阅输出请求流

1. 客户端访问 `/s/:token/mihomo` 或 `/s/:token/singbox`
2. Worker 校验 token 和用户状态
3. 优先读取 KV 缓存
4. 缓存命中则直接返回
5. 缓存未命中则读取 D1 所需配置
6. 使用 `packages/core` 生成最终订阅内容
7. 写入 KV 后返回客户端

#### 定时同步请求流

1. Cron 触发同步任务
2. Worker 拉取启用中的远程订阅自动同步源
3. 解析节点、去重并校验链式代理
4. 更新 `nodes` 与自动同步源最近状态
5. 清理受影响用户的预览 / 订阅缓存

---

## 6. 模块设计

### 6.1 管理员认证模块

MVP 建议使用最简单且可维护的方案：

- 管理员账号存 D1
- 密码存 hash，不存明文
- 登录成功后签发会话 token
- token 通过 HTTP-only cookie 或 Bearer token 传递

MVP 推荐：

- API 侧支持 Bearer token
- Web 侧当前默认使用 Bearer token 会话

后续如要增强，可引入：

- Cloudflare Access
- 单点登录
- 多角色权限模型

### 6.2 用户与订阅模块

每个用户至少拥有：

- 唯一 token
- 状态（启用 / 禁用）
- 到期时间
- 可用节点集合
- 可用模板集合

订阅生成时必须校验：

- token 是否存在
- 用户是否启用
- 是否已过期
- 是否存在可用节点
- 是否存在目标格式模板

### 6.3 节点管理模块

节点来源分两类：

- 手动录入
- 远程同步导入

节点必须统一存储为规范化结构，而不是把各种原始文本直接散落保存。推荐字段包括：

- 节点名称
- 协议类型
- 服务地址
- 端口
- 凭据 / UUID / 密钥等配置
- 附加参数 JSON
- 来源类型
- 启用状态
- 最后同步时间

### 6.4 模板管理模块

模板用于定义最终配置的骨架，不允许在运行期执行任意脚本。模板模型建议：

- `target_type`：`mihomo` 或 `singbox`
- `content`：模板正文
- `version`：版本号
- `is_default`：是否默认模板
- `enabled`：是否启用

模板渲染由 `packages/core` 负责，Web 只负责编辑与预览。

### 6.5 远程订阅自动同步源模块

当前长期自动同步对象只有 `remote_subscription_sources`，至少包括：

- 名称
- URL
- 是否启用
- 最后同步时间
- 最近同步状态
- 最近同步消息
- 最近同步详情
- 失败次数

同步时需要：

- 拉取上游内容
- 解析分享链接 / Base64 订阅 / YAML / JSON 节点
- 做节点去重与链式代理校验
- 成功时更新 `nodes` 与同步源状态详情
- 失败时只写 `last_sync_*` / `failure_count`，不覆盖现有有效结果

当前设计基线里不再包含独立的 `rule_sources`、`rule_snapshots` 或 `sync_logs` 业务链路。

### 6.6 缓存模块

缓存对象至少包括：

- `sub:v2:mihomo:{token}`
- `sub:v2:singbox:{token}`
- `preview:v2:mihomo:{userId}`
- `preview:v2:singbox:{userId}`
- `rate:admin-login:{clientKey}:{username}`
- `rate:subscription:{target}:{token}:{clientKey}`

缓存策略：

- 用户、节点、模板、自动同步源变更后主动失效
- 预览与订阅请求都采用读缓存优先
- 对订阅结果设置 TTL，避免长期脏数据

补充说明：

- `packages/shared` 里仍保留 `template:default:*` / `rules:*` 相关 cache helper 作为兼容导出
- 但当前 Worker 主链路只把 preview / subscription / rate-limit key 视为运行时设计基线

MVP 推荐订阅缓存 TTL：

- 15 分钟到 60 分钟之间

建议默认值：

- 30 分钟

### 6.7 同步与任务模块

Cron 负责：

- 同步远程订阅自动同步源
- 更新同步状态与诊断详情
- 清理受影响用户缓存

MVP 先支持：

- 手动同步
- 固定频率的自动同步

不做：

- 独立规则快照链路
- 更通用的远程节点源持久化对象与复杂任务编排
- 多阶段任务队列

---

## 7. 数据模型设计

以下为建议的首版 D1 表结构。

### 7.1 `admins`

用途：管理员账号

建议字段：

- `id` TEXT PRIMARY KEY
- `username` TEXT UNIQUE NOT NULL
- `password_hash` TEXT NOT NULL
- `role` TEXT NOT NULL DEFAULT 'admin'
- `status` TEXT NOT NULL DEFAULT 'active'
- `created_at` TEXT NOT NULL
- `updated_at` TEXT NOT NULL

### 7.2 `users`

用途：订阅用户

建议字段：

- `id` TEXT PRIMARY KEY
- `name` TEXT NOT NULL
- `token` TEXT UNIQUE NOT NULL
- `status` TEXT NOT NULL DEFAULT 'active'
- `expires_at` TEXT NULL
- `remark` TEXT NULL
- `created_at` TEXT NOT NULL
- `updated_at` TEXT NOT NULL

索引建议：

- `UNIQUE(token)`
- `INDEX(status)`
- `INDEX(expires_at)`

### 7.3 `nodes`

用途：节点统一存储

建议字段：

- `id` TEXT PRIMARY KEY
- `name` TEXT NOT NULL
- `protocol` TEXT NOT NULL
- `server` TEXT NOT NULL
- `port` INTEGER NOT NULL
- `credentials_json` TEXT NULL
- `params_json` TEXT NULL
- `source_type` TEXT NOT NULL DEFAULT 'manual'
- `source_id` TEXT NULL
- `enabled` INTEGER NOT NULL DEFAULT 1
- `last_sync_at` TEXT NULL
- `created_at` TEXT NOT NULL
- `updated_at` TEXT NOT NULL

索引建议：

- `INDEX(enabled)`
- `INDEX(protocol)`
- `INDEX(source_type, source_id)`

### 7.4 `templates`

用途：订阅模板

建议字段：

- `id` TEXT PRIMARY KEY
- `name` TEXT NOT NULL
- `target_type` TEXT NOT NULL
- `content` TEXT NOT NULL
- `version` INTEGER NOT NULL DEFAULT 1
- `is_default` INTEGER NOT NULL DEFAULT 0
- `enabled` INTEGER NOT NULL DEFAULT 1
- `created_at` TEXT NOT NULL
- `updated_at` TEXT NOT NULL

索引建议：

- `INDEX(target_type, enabled)`
- `INDEX(target_type, is_default)`

### 7.5 `user_node_map`

用途：用户与节点的关联关系

建议字段：

- `id` TEXT PRIMARY KEY
- `user_id` TEXT NOT NULL
- `node_id` TEXT NOT NULL
- `enabled` INTEGER NOT NULL DEFAULT 1
- `created_at` TEXT NOT NULL

索引建议：

- `UNIQUE(user_id, node_id)`
- `INDEX(user_id)`
- `INDEX(node_id)`

### 7.6 `remote_subscription_sources`

用途：长期自动同步的上游订阅源

建议字段：

- `id` TEXT PRIMARY KEY
- `name` TEXT NOT NULL
- `source_url` TEXT NOT NULL UNIQUE
- `enabled` INTEGER NOT NULL DEFAULT 1
- `last_sync_at` TEXT NULL
- `last_sync_status` TEXT NULL
- `last_sync_message` TEXT NULL
- `last_sync_details_json` TEXT NULL
- `failure_count` INTEGER NOT NULL DEFAULT 0
- `created_at` TEXT NOT NULL
- `updated_at` TEXT NOT NULL

### 7.7 `audit_logs`

用途：管理员操作审计

建议字段：

- `id` TEXT PRIMARY KEY
- `actor_admin_id` TEXT NOT NULL
- `action` TEXT NOT NULL
- `target_type` TEXT NOT NULL
- `target_id` TEXT NULL
- `payload_json` TEXT NULL
- `created_at` TEXT NOT NULL

---

## 8. API 设计

API 分为三类：

- 健康检查接口
- 受保护管理接口
- 公开订阅接口

### 8.1 健康检查

#### `GET /health`

用途：服务健康检查

返回建议：

```json
{
  "ok": true,
  "service": "subforge",
  "time": "2026-03-07T00:00:00.000Z"
}
```

### 8.2 管理员认证接口

#### `POST /api/admin/login`

用途：管理员登录

请求示例：

```json
{
  "username": "admin",
  "password": "******"
}
```

成功返回：

```json
{
  "ok": true,
  "token": "session-token",
  "admin": {
    "id": "adm_001",
    "username": "admin",
    "role": "admin"
  }
}
```

#### `GET /api/admin/me`

用途：获取当前登录管理员信息

#### `POST /api/admin/logout`

用途：退出登录

### 8.3 用户管理接口

#### `GET /api/users`

用途：用户列表

#### `POST /api/users`

用途：创建用户

建议字段：

- `name`
- `expires_at`
- `remark`

#### `PATCH /api/users/:id`

用途：更新用户

#### `POST /api/users/:id/reset-token`

用途：重置 token

#### `POST /api/users/:id/nodes`

用途：绑定用户可用节点

### 8.4 节点管理接口

#### `GET /api/nodes`

用途：节点列表

#### `POST /api/nodes`

用途：创建节点

#### `PATCH /api/nodes/:id`

用途：更新节点

#### `DELETE /api/nodes/:id`

用途：删除节点

### 8.5 模板管理接口

#### `GET /api/templates`

#### `POST /api/templates`

#### `PATCH /api/templates/:id`

#### `POST /api/templates/:id/set-default`

### 8.6 自动同步源接口

#### `GET /api/remote-subscription-sources`

#### `POST /api/remote-subscription-sources`

#### `PATCH /api/remote-subscription-sources/:id`

#### `POST /api/remote-subscription-sources/:id/sync`

用途：维护长期自动同步的远程订阅源

### 8.7 订阅预览接口

#### `GET /api/preview/:userId/mihomo`

#### `GET /api/preview/:userId/singbox`

用途：受保护预览生成结果，不走公开 token 路径

### 8.8 公开订阅接口

#### `GET /s/:token/mihomo`

返回：`text/plain` 或 YAML

#### `GET /s/:token/singbox`

返回：`application/json`

### 8.9 接口错误格式

建议统一返回：

```json
{
  "ok": false,
  "error": {
    "code": "USER_DISABLED",
    "message": "user is disabled"
  }
}
```

---

## 9. 订阅生成设计

### 9.1 生成流程

订阅生成服务需要执行以下步骤：

1. 根据 token 查找用户
2. 校验用户状态和到期时间
3. 查询用户绑定的可用节点
4. 读取目标格式默认模板
5. 组装标准中间数据模型
6. 渲染为目标格式文本
7. 写入缓存并返回

### 9.2 中间模型

为了避免不同输出格式直接耦合数据库结构，`packages/core` 需要定义统一中间模型，例如：

- `SubscriptionUser`
- `SubscriptionNode`
- `SubscriptionTemplate`
- `CompiledSubscription`

补充说明：

- 当前主链路不再携带独立规则源对象
- 模板骨架和渲染器默认规则兜底，是当前公开订阅稳定性的主保障

### 9.3 渲染器设计

推荐按目标格式拆成两个 renderer：

- `renderMihomoSubscription()`
- `renderSingboxSubscription()`

两者共享统一输入模型，但输出格式独立，避免后续维护混乱。

### 9.4 模板策略

模板必须是“有限变量替换 + 明确结构插槽”，不允许执行任意脚本。支持的插槽示例：

- `{{proxies}}`
- `{{proxy_groups}}`
- `{{rules}}`
- `{{dns}}`
- `{{outbounds}}`

---

## 10. 同步设计

### 10.1 同步对象

MVP 先支持：

- 远程订阅自动同步源同步

当前写实边界：

- `remote_subscription_sources` 是当前唯一长期自动同步模型
- 一次性远程导入走 `node-import/preview`，不进入长期同步对象
- 更通用的远程节点源对象已不再作为当前产品范围

### 10.2 同步流程

1. 读取启用的远程订阅自动同步源配置
2. 请求远程 URL
3. 校验响应状态码与内容类型
4. 解析远程订阅节点内容
5. 转换为内部标准结构并比较指纹 / 去重结果
6. 写入 `nodes`
7. 更新 `remote_subscription_sources` 最近同步状态
8. 失效相关缓存，并刷新受影响绑定

### 10.3 失败处理

同步失败时必须：

- 更新最近一次失败状态和详情
- 增加失败计数
- 保留现有有效节点与绑定
- 只有新结果校验通过后才替换或禁用旧节点

---

## 11. 缓存与失效策略

### 11.1 缓存 Key 约定

建议使用以下 key：

- `sub:v2:mihomo:{token}`
- `sub:v2:singbox:{token}`
- `preview:v2:mihomo:{userId}`
- `preview:v2:singbox:{userId}`
- `rate:admin-login:{clientKey}:{username}`
- `rate:subscription:{target}:{token}:{clientKey}`

### 11.2 失效触发点

以下操作必须触发缓存失效：

- 用户状态变化
- 用户 token 重置
- 用户绑定节点变化
- 节点内容变化
- 默认模板变化
- 自动同步源同步完成

### 11.3 缓存原则

- 公开订阅接口可缓存
- 管理员预览接口可短缓存或不缓存
- 缓存失效优先保证正确性，其次才是命中率

---

## 12. 安全设计

### 12.1 基本安全要求

MVP 需要满足：

- 密码 hash 存储
- 管理接口必须鉴权
- 输入参数必须校验
- 敏感信息不得写入前端明文
- 日志中不得记录完整密码与私钥

### 12.2 Token 安全

- 用户 token 使用高熵随机字符串
- 支持重置 token
- token 泄露后可立即失效旧 token

### 12.3 风险边界

MVP 不实现复杂 RBAC，但需要预留 `role` 字段；
后续如果扩展多管理员，再增加细粒度权限。

---

## 13. 配置与部署原则

### 13.1 环境变量原则

Worker 环境变量建议至少包含：

- `ADMIN_JWT_SECRET`
- `SUBSCRIPTION_CACHE_TTL`
- `SYNC_HTTP_TIMEOUT_MS`
- `APP_ENV`

与限流、预览缓存相关的具体变量和默认值，应以 `README.md` 与 `docs/部署指南.md` 为准，避免在设计文档里重复维护一份操作手册。

### 13.2 Cloudflare 绑定原则

设计上需要绑定：

- D1：主数据库
- KV：订阅缓存与轻量限流计数
- Cron：同步任务

可选绑定：

- R2：备份
- Access / Zero Trust：管理入口保护

### 13.3 文档边界

与部署相关的内容建议按以下边界维护：

- `docs/实施方案.md`：只描述部署设计原则与架构取舍
- `docs/部署指南.md`：维护本地启动、Cloudflare 配置、部署顺序与部署后验收
- `docs/运维Runbook与告警处理.md`：维护巡检、告警和事件响应流程
- `docs/排障与常见问题.md`：维护 `Unexpected token '<'`、`429`、缓存命中异常等症状驱动排查

## 14. 演进阶段参考

下面的阶段用于表达**推荐的演进顺序**，而不是当前仓库的实时待办列表：

### Phase 0：仓库与基础设施

目标：建立可开发、可构建、可部署的基础骨架。

### Phase 1：共享层与核心层

目标：先稳定类型、错误码、渲染接口与订阅编译中间模型。

### Phase 2：数据库与 Worker API

目标：完成 D1 schema、访问层与核心 API 能力。

### Phase 3：前端管理界面

目标：让系统可在浏览器内完成主要配置流程。

### Phase 4：同步与缓存完善

目标：补齐同步、缓存、失效与观测链路。

### Phase 5：增强与上线准备

目标：补齐审计、回归验证、自动化部署与运维配套。

当前进展请查看：

- `README.md`
- `CHANGELOG.md`
- `docs/仓库清理与收敛计划.md`

## 15. 实施优先级

建议按以下顺序推进：

1. 先稳定 `shared` 与 `core`
2. 再稳定 D1 schema 与 Worker API
3. 再完善 Web 前端主界面
4. 再补同步、缓存、审计与观测
5. 最后补自动化部署与长期运维能力

原因是：

- 订阅生成与数据模型是项目核心
- UI 应建立在稳定协议与数据结构之上
- 运维与自动化应建立在可运行、可验证的主链路之上

## 16. 风险与规避

### 风险 1：模板能力过于灵活导致系统失控

规避：

- 不支持运行任意 JS
- 只支持受控占位符与结构化渲染

### 风险 2：同步失败覆盖有效配置

规避：

- 保留最后一次成功快照
- 新结果校验失败时不覆盖现有快照

### 风险 3：数据库结构过早复杂化

规避：

- MVP 先保留必要表
- 高级特性通过新增表和字段扩展

### 风险 4：订阅缓存导致脏数据

规避：

- 明确失效触发点
- 对关键修改路径统一执行缓存清理

## 17. 实施跟踪方式

为了避免“设计文档”和“操作文档”重复维护，建议这样使用：

- `docs/实施方案.md`：长期设计基线、模块边界、架构原则
- `README.md`：项目入口、快速开始、能力摘要
- `docs/部署指南.md`：本地启动、Cloudflare 部署、部署后验收
- `CHANGELOG.md`：版本变更与已完成项记录
- `docs/仓库清理与收敛计划.md`：当前清理批次、执行顺序与下一步建议

## 18. 本文档结论

SubForge 的正确落地路径不是“一次性做完整平台”，而是：

- 先稳定订阅生成与数据模型
- 再稳定 Worker API 与前端配置链路
- 再补同步、缓存、日志与观测
- 最后补自动化部署与长期运维能力

因此，这份文档应持续扮演“设计基线”角色，而不是实时操作手册或发布记录。
