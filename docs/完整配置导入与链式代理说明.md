# 文档已合并

当前版本不再把“完整配置导入、协议支持矩阵、链式代理专题”拆成多份长文档维护。

请直接看：

- `docs/单用户托管模式需求.md`
- `docs/节点管理与订阅使用说明.md`

如果你只是要确认当前是否支持：

- 完整 Mihomo / Clash YAML 导入
- 完整 sing-box JSON 导入
- `dialer-proxy` / `detour` 到 `params.upstreamProxy` 的映射

以上内容都已经收敛到上述两份主文档中。

本页仅为兼容旧链接保留。
